version: '3'
services:
  redis:
    image: redis:7.2
    ports:
      - "6379:6379"
    volumes:
      - ./data/redis/:/data
    command: redis-server --appendonly yes

networks:
  default:
    driver: bridge
