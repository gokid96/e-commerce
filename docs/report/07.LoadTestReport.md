# 부하 테스트 보고서

## 배경

이커머스 시스템의 핵심 비즈니스 기능에 대해 고가용성(High Availability)을 확보하기 위해 부하 테스트를 수행한다.
이번 테스트를 통해 병목 지점을 사전에 파악하여 성능 저하를 방지하고,
테스트 결과를 기반으로 한 TPS(Transactions Per Second)를 기준으로 적정 서버 Pod 수와 필요한 리소스를 산정할 수 있다.
또한, 실제 트래픽 상황을 재현하여 장애 발생 구간을 사전에 점검함으로써 시스템 안정성을 확보하고자 한다.

## 대상 선정

부하 테스트의 대상은 시스템의 핵심 비즈니스 기능을 기준으로 선정하였다.
이번 테스트에서는 주문/결제 시나리오와 선착순 쿠폰 발급 시나리오를 중심으로 진행한다.

### 주문/결제

주문/결제는 사용자 경험과 직접적으로 연결된 핵심 서비스 기능으로,
실제 사용 흐름을 기반으로 시나리오를 구성하여 부하 테스트를 수행하였다.

테스트 시나리오는 다음과 같은 단계로 구성된다.

1. 인기상품 조회
2. 잔액 충전
3. 잔액 조회
4. 주문/결제 진행
5. 주문 완료 상태 확인

실제 사용자 전체가 상품을 조회한 후 주문까지 진행하지는 않기 때문에,
현실적인 사용자 행동 양상을 반영하여 다음과 같은 가정을 두고 테스트를 구성하였다.

- 전체 사용자 중 20% 사용자만 잔액 충전
- 충전 사용자 중 10% 사용자만 주문/결제

이와 같은 조건을 통해 정상적인 비즈니스 흐름을 유지하면서도 실제 트래픽 양상에 가까운 테스트를 설계하였다.

### 선착순 쿠폰 발급

선착순 쿠폰 발급 기능은 시스템 내에서 가장 높은 단일 시점 트래픽이 집중되는 기능이다.
이 기능의 안정성을 검증하기 위해, 고부하 상황을 집중적으로 발생시키는 Peak Test 방식으로 시나리오를 구성하였다.

특정 시점에 다수의 사용자가 동시에 쿠폰을 요청하는 상황을 재현하여,
트래픽 집중 시의 응답 시간, 처리율, 에러 발생 여부 등을 중점적으로 검증한다.
이를 통해 트래픽 폭주 상황에서도 시스템이 안정적으로 동작하는지 확인하고자 한다.

## 테스트 환경 구축

테스트 환경은 로컬 PC의 도커 컨테이너를 활용하여 구성하였다.

### Spring 테스트 환경

다음은 스프링 애플리케이션 `docker-compose.yml` 일부이다.

```yaml
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - TZ=Asia/Seoul
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4G
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
```

Spring 애플리케이션의 리소스를 제한하기 위해 CPU는 2 vCPU, 메모리는 4 GB로 설정하였다.
추가로, Spring Actuator를 통해 메트릭을 수집하고, Prometheus 및 Grafana와 연동하여 모니터링 대시보드를 구성하였다.

#### `build.gradle`의 의존성 추가

```groovy
dependencies {
    // ... 생략 ...

    // Actuator
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    implementation 'io.micrometer:micrometer-registry-prometheus'
}
```

#### `prometheus.yml` 추가

```yaml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'spring-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: [ 'api:8080' ]
```

#### `application.yaml`의 actuator 설정 추가

```yaml
management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    enable:
      all: true
```

### K6 테스트 환경

부하 테스트 도구로는 K6를 사용하였으며, InfluxDB 및 Grafana와 연동하여 테스트 결과를 시각화하였다.
다음은 InfluxDB와 Grafana를 위한 `docker-compose.yml` 일부이다.

```yaml
services:
  influxdb:
    image: influxdb:1.8
    ports:
      - "8086:8086"
    environment:
      - INFLUXDB_DB=k6

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_ANONYMOUS_ENABLED=true
```

K6 실행 시 `--out influxdb=http://localhost:8086/k6` 옵션으로 측정 지표를 InfluxDB로 전송하고, Grafana 대시보드에서 API별 응답 시간(P95/P99)을 관찰하였다.
이와 같이 로컬 Docker 기반의 통합 테스트 및 모니터링 환경을 구축하였다.

## 테스트 설계 및 목적

### 주문/결제

#### 테스트 시나리오

주문/결제 기능은 단기간 폭증보다는 일정 수준의 트래픽이 지속적으로 유지되는 특성이 있으므로,
Load Test 방식으로 최대 300 Virtual Users(VU)까지 점진적으로 증가시키는 테스트를 설계하였다.

1. 10초 동안 100 VU 까지 증가
2. 10초 동안 200 VU 까지 증가
3. 10초 동안 300 VU 까지 증가
4. 30초 동안 300 VU 유지
5. 10초 동안 100 VU 까지 감소
6. 10초 동안 50 VU 까지 감소
7. 10초 동안 0 VU 까지 감소

#### 목표 TPS

| 기능 | 사용자 기준 | 처리 빈도 | 목표 TPS |
|---|---|---|---|
| 인기상품 조회 | 100명 | 1초에 1건 | 100 TPS |
| 잔액 충전 | 100명 | 10초에 1건 | 10 TPS |
| 주문/결제 | 100명 | 100초에 1건 | 1 TPS |

#### 테스트 데이터

부하 테스트에 필요한 더미 데이터를 사전에 적재하였다.

- 상품 및 재고 데이터: 각 10,000건
- 잔액 및 사용자 데이터: 각 10,000건
- 인기상품 랭킹: Redis ZSET(`rank:sell:{yyyyMMdd}`)에 상위 상품 사전 적재
- 인기상품 랭킹(캐싱 미적용 조회 경로용): `product_rank` 테이블 1,000건

인기상품 조회는 Redis ZSET과 캐싱으로 이미 구성되어 있으며, 캐싱 효과를 정량 검증하기 위해 캐싱 미적용(DB 직접 조회) 경로를 대조군으로 함께 측정하였다.

#### 테스트 스크립트

`k6/order_payment.js`를 사용하여 인기상품 조회 → 충전/조회 → 주문/주문확인 순으로 요청하며, 각 요청에 `tags{name}`을 부여하여 API별로 응답 시간을 분리 집계하였다.

### 선착순 쿠폰 발급

#### 테스트 시나리오

선착순 쿠폰 발급 기능은 짧은 시간에 트래픽이 폭증하는 특성이 있으므로,
Peak Test 방식으로 최대 1000 VU까지 두 차례 급증시키는 테스트를 설계하였다.

1. 10초 동안 10 VU 유지
2. 10초 동안 10 VU 유지
3. 10초 동안 700 VU 까지 증가
4. 10초 동안 10 VU 까지 감소
5. 10초 동안 10 VU 유지
6. 10초 동안 1000 VU 까지 증가
7. 10초 동안 10 VU 까지 감소
8. 10초 동안 0 VU 까지 감소

#### 목표 TPS

| 기능 | 사용자 기준 | 처리 빈도 | 목표 TPS |
|---|---|---|---|
| 쿠폰 발급 | 100명 | 1초에 1건 | 100 TPS |

#### 테스트 데이터

- 쿠폰: 발급 가능 상태 1건 (수량 100,000)
- 발급 가능 플래그: Redis(`coupon_available:{couponId}`)에 사전 적재

쿠폰 발급은 Kafka 비동기 발행으로 이미 구성되어 있으며, 비동기 처리의 효과를 정량 검증하기 위해 동기 발급(요청 시점 즉시 처리) 경로를 대조군으로 함께 측정하였다.

#### 테스트 스크립트

`k6/coupon_issue.js`를 사용하여 각 VU가 고유 사용자 ID로 쿠폰 발급을 요청하였다.

## 테스트 결과 및 분석

### 주문/결제

아래는 주문/결제 기능에 대해 인기상품 조회를 캐싱 미적용(DB 직접 조회) 상태로 수행한 결과 요약이다.

#### K6 CLI 명령어

```bash
k6 run --out influxdb=http://localhost:8086/k6 k6/order_payment.js --summary-trend-stats="avg,min,med,max,p(50),p(90),p(95),p(99),p(99.9)"
```

#### K6 결과 요약

```text
     █ 주문/결제 시나리오

     checks.........................: 98.70%
     http_req_duration..............: avg=375ms  med=201ms  p(90)=913ms  p(95)=1.39s  p(99)=2.05s  p(99.9)=3.04s
     http_reqs......................: 9470   102.36/s
     iterations.....................: 6576   71.08/s
     vus............................: max=300
```

#### 결과 분석

##### 주요 API별 응답 시간 분포 (P95)

| API | P95 |
|---|---|
| 인기상품 조회 | 1.38s |
| 잔액 충전 | 1.30s |
| 주문 상태 확인 | 1.27s |
| 주문/결제 | 1.20s |
| 잔액 조회 | 1.00s |

- 전체 HTTP 요청의 P99 응답 시간은 2.05초로 측정되어, SLA(1초 이내)를 초과하였다.
- 인기상품 조회가 매 요청 DB(`product_rank` 조회 + 상품 개별 조회)로 처리되며 커넥션 경합이 발생하고, 잔액 충전 및 주문 확인 등 다른 API까지 동반 지연되어 시나리오 전체가 성능 병목 지점으로 판단된다.

### 선착순 쿠폰 발급

아래는 선착순 쿠폰 발급 기능에 대해 동기 발급 상태로 수행한 결과 요약이다.

#### K6 CLI 명령어

```bash
k6 run --out influxdb=http://localhost:8086/k6 k6/coupon_issue.js --summary-trend-stats="avg,min,med,max,p(50),p(90),p(95),p(99),p(99.9)"
```

#### K6 결과 요약

```text
     █ 쿠폰발급

     checks.........................: 100.00%
     http_req_duration..............: avg=1.79s  med=1.42s  p(90)=3.82s  p(95)=4.68s  p(99)=5.07s  p(99.9)=5.12s
     http_reqs......................: 5662   70.63/s
     iterations.....................: 5662   70.63/s
     vus............................: max=1000
```

#### 결과 분석

##### 주요 API 응답 시간 분포

| API | AVG | MAX | P95 | P99 |
|---|---|---|---|---|
| 쿠폰 발급 | 1.66s | 5.14s | 4.57s | 5.07s |

전체 요청의 P99가 5.07초로 측정되어 SLA(1초 이내)를 크게 초과하였다.
동기 발급 방식에서 1000 VU가 동일 쿠폰에 동시 접근하며 행(row) 경합이 발생하는 병목 현상으로, 성능 튜닝 및 병목 원인 분석이 필요하다.

## 성능 개선

### 주문/결제

주문/결제 결과에서 P99 기준 2초 이상의 응답 지연이 발생하여 병목 지점이 확인되었다.
이를 해결하기 위해 다음과 같은 성능 개선을 적용하였다.

- 인기상품 조회 API: Redis ZSET 랭킹 조회 및 Redis 캐싱(`@Cacheable`) 적용

개선 후 동일한 테스트 시나리오로 재측정한 결과는 다음과 같다.

#### K6 결과 요약

```text
     █ 주문/결제 시나리오

     checks.........................: 98.70%
     http_req_duration..............: avg=5.68ms  med=2.99ms  p(90)=17.7ms  p(95)=20.58ms  p(99)=27.26ms  p(99.9)=40.05ms
     http_reqs......................: 11904   130.00/s
     iterations.....................: 8284    90.47/s
     vus............................: max=300
```

#### 결과 분석 및 개선

##### 주요 API별 응답 시간 (P95)

| 구간 | 개선 전 | 개선 후 | 개선율 |
|---|---|---|---|
| 인기상품 조회 | 1.38s | 4.46ms | 99.7% |
| 잔액 충전 | 1.30s | 28.0ms | 97.8% |
| 주문 상태 확인 | 1.27s | 6.74ms | 99.5% |
| 주문/결제 | 1.20s | 24.0ms | 98.0% |
| 잔액 조회 | 1.00s | 6.59ms | 99.3% |

##### 최종 요약 비교

| 지표 항목 | 개선 전 | 개선 후 | 개선 효과 |
|---|---|---|---|
| 전체 요청 수 | 9,470 | 11,904 | 약 25.7% 증가 |
| 평균 응답시간 | 375 ms | 5.68 ms | 약 98.5% 감소 |
| P99 응답시간 | 2.05 s | 27.26 ms | 약 98.7% 개선 |
| P99.9 응답시간 | 3.04 s | 40.05 ms | 약 98.7% 개선 |

##### TPS 분석

| 항목 | 결과 TPS | 목표 TPS | 달성 여부 |
|---|---|---|---|
| 인기상품 조회 | 약 92 TPS | 100 TPS | 근접 (스케일 아웃 시 보완 가능) |
| 잔액 충전 | 약 18 TPS | 10 TPS | 초과 달성 |
| 주문/결제 | 약 2 TPS | 1 TPS | 초과 달성 |

인기상품 조회를 DB 처리에서 Redis 캐싱으로 전환하자 DB 부하가 해소되면서, 해당 API뿐 아니라 잔액 충전 및 주문 등 시나리오 전체가 밀리초 단위로 회복되었다.

### 선착순 쿠폰 발급

선착순 쿠폰 발급에 대한 Peak Test 결과, P99 기준 5초 이상의 응답 지연이 확인되어 병목 지점이 존재함을 확인하였다.
이를 해결하기 위해 다음과 같은 성능 개선 작업을 수행하였다.

- 쿠폰 발급 요청 흐름에 Kafka 비동기 처리 도입 (요청은 발급 가능 여부만 확인하고 즉시 이벤트를 발행하며, 실제 수량 차감 및 발급은 컨슈머가 처리)

특히, 발급 요청이 이벤트 발행만 수행하고 즉시 응답하게 되면서, 동일 쿠폰에 대한 행 경합이 응답 경로에서 제거되었다.

#### K6 결과 요약

```text
     █ 쿠폰발급

     checks.........................: 100.00%
     http_req_duration..............: avg=2.92ms  med=2.7ms  p(90)=3.63ms  p(95)=4.01ms  p(99)=9.96ms  p(99.9)=26.54ms
     http_reqs......................: 9769   121.46/s
     iterations.....................: 9769   121.46/s
     vus............................: max=1000
```

#### 결과 분석 및 개선

##### 쿠폰 발급 응답 시간

| 구간 | 개선 전 | 개선 후 | 개선율 |
|---|---|---|---|
| 평균 | 1.66s | 2.92ms | 99.8% |
| P95 | 4.57s | 4.01ms | 99.9% |
| P99 | 5.07s | 9.96ms | 99.8% |
| P99.9 | 5.12s | 26.54ms | 99.5% |

##### 최종 요약 비교

| 지표 항목 | 개선 전 | 개선 후 | 개선 효과 |
|---|---|---|---|
| 전체 요청 수 | 5,662 | 9,769 | 약 72.5% 증가 |
| 평균 응답시간 | 1.66s | 2.92ms | 약 99.8% 감소 |
| P99 응답시간 | 5.07s | 9.96ms | 약 99.8% 개선 |
| P99.9 응답시간 | 5.12s | 26.54ms | 약 99.5% 개선 |

##### TPS 분석

| 구간 | TPS | 비고 |
|---|---|---|
| 개선 전 (동기) | 약 70 TPS | 목표 100 TPS 미달 |
| 개선 후 (비동기) | 약 121 TPS | 목표 초과 달성 |

TPS는 기존 70 TPS에서 121 TPS로 증가하여 목표 TPS(100)를 초과 달성하였다.
다만, 본 기능은 이벤트성 트래픽 특성이 강하므로, 운영 환경에서는 Pod 수를 스케일 아웃하여 탄력적으로 대응할 필요가 있다.

## 결론

이번 부하 테스트를 통해 주문/결제 및 선착순 쿠폰 발급 기능의 주요 성능 병목 지점을 식별하고,
이에 대한 적절한 개선 조치를 수행함으로써 전체적인 응답 속도 및 처리량을 크게 향상시킬 수 있었다.

주요 개선 사항은 다음과 같다.

- 주문/결제: 인기상품 조회에 Redis ZSET 랭킹 조회 및 Redis 캐싱 적용
- 쿠폰 발급: Kafka 비동기 처리 도입으로 응답 경로의 행 경합 제거

이와 같이 부하 테스트 기반의 사전 검증을 통해 병목 지점을 조기에 식별하고,
적절한 대응으로 운영 중 발생 가능한 장애를 효과적으로 예방할 수 있었다.

향후 운영 환경에서는 다음과 같은 추가적인 방어 체계를 통해 장애 예방 효과를 더욱 극대화할 수 있다.

- 로깅: 병목 또는 예외 발생 시 추적 가능한 구조 확보
- 서킷 브레이커 적용: 외부 서비스 지연 시 시스템 안정성 유지
- 모니터링 및 알림 시스템: 지표 기반 실시간 감지 및 대응 체계 강화
